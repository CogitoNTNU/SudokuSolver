generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("DB_URI")
}

model Image {
  id    Int @id @default(autoincrement())
  data Bytes
  label Int?
}